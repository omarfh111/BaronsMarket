# ============================================================
# Secure Face Access System - Requirements
# Python 3.12 REQUIRED (pre-compiled dlib wheel only available for 3.12)
# Install with: pip install -r requirements.txt
# ============================================================

# --- Core Vision & ML ---
opencv-python==4.10.0.84
numpy<2.0                        # numpy>=2.0 breaks deepface & face-recognition

# --- Face Recognition (dlib-dependent) ---
# IMPORTANT: dlib must be installed BEFORE face-recognition.
# On Windows, download the pre-built wheel from:
# https://github.com/z-mahmud22/Dlib_Windows_Python3.x
# Then run: pip install dlib-<version>-cp312-cp312-win_amd64.whl
# ONLY THEN run: pip install face-recognition==1.3.0
face-recognition==1.3.0

# --- Deep Learning ---
deepface==0.0.93                 # Pin to tested version; newer versions may change API
tf-keras                         # Required by deepface (TensorFlow 2.x Keras backend)
torch==2.3.1                     # Pin to tested version
torchvision==0.18.1              # Must match torch version above

# --- OCR ---
easyocr==1.7.1

# --- Utilities ---
python-dotenv
setuptools==69.5.1
easydict
tqdm
Pillow
tensorboardX
scipy
scikit-image
